// Environnement par défaut / production (cours Angular : fichiers d'environnement).
export const environment = {
  production: true,
  // URL de base de l'API .NET
  apiUrl: 'http://localhost:5252/api',
};
