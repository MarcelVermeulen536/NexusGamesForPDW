// Environnement de développement (cours Angular : fichiers d'environnement).
// `ng serve` utilise ce fichier (fileReplacements dans angular.json).
export const environment = {
  production: false,
  // URL de base de l'API .NET (profil http de launchSettings.json)
  apiUrl: 'http://localhost:5252/api',
};
